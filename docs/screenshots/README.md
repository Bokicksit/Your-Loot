# Screenshots

The README links three files from this directory. They don't exist yet —
they have to come off a real install with a real collection in it, because
an empty app photographs as an empty app.

| File | What to capture |
|---|---|
| `collection.png` | A cards grid with plenty in it, three across, showing owned frames and copy chips |
| `scan.png` | The barcode scanner open with the framing window over an actual box |
| `pokedex.png` | The binder view, part filled — the gaps are the point |

Phone-shaped portrait shots, since that's how the app is used. Crop the
browser chrome out. Keep them under about 400KB each; the README loads them
side by side and GitHub doesn't resize.

Delete this file once they're in.
